# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'zoopedia' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Material', '~> 2.0'
  pod 'SwiftyJSON'
  pod 'Alamofire', '~> 4.5'
  pod 'GoogleMaps'
  pod 'SwiftSpinner'
  # Pods for zoopedia

  target 'zoopediaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'zoopediaUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
